#!/bin/bash
# =========================================================================
# 文件作用：停止并移除所有容器（保留数据卷）
# 使用方式：在项目根目录执行 ./scripts/stop.sh
# =========================================================================
set -e
cd "$(dirname "$0")/.."
docker-compose down
echo "服务已停止。"
