#!/bin/bash
# =========================================================================
# 文件作用：一键启动全部服务（构建并后台运行）
# 使用方式：在项目根目录执行 ./scripts/start.sh 或 bash scripts/start.sh
# =========================================================================
set -e
cd "$(dirname "$0")/.."
echo "正在构建并启动服务..."
docker-compose up -d --build
echo "启动完成。访问 http://localhost 使用系统。"
